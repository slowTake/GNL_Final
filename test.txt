int	main(void)
{
	int fd;
	char *line;
	int line_count;

	fd = open("test.txt", O_RDONLY);
	line_count = 0;
	while ((line = get_next_line(fd)) != NULL)
	{
		printf("%s", line);
		free(line);
	}
	close(fd);
	return (0);
}
